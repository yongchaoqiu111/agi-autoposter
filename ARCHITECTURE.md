# AGI AutoPoster - 架构设计文档

## 核心设计：三层结构体组合

本项目不是"任务队列系统"，而是**三层结构体组合的标准实现**。

每个"事件"由三层结构体组成，可以无限嵌套组合成更大的事件。

---

## 一、三层结构体定义

### 1. 内容层（Content Layer）

| 项目 | 说明 |
|------|------|
| 职责 | 生成内容、审核内容、存储内容 |
| 输入 | 任务需求、主题、风格 |
| 输出 | content_id（文案ID + 图片ID） |
| 特性 | 独立运行，不依赖其他层 |

**关键原则**：大模型只选择已有的 content_id，不编造内容。

### 2. 调度层（Scheduler Layer）

| 项目 | 说明 |
|------|------|
| 职责 | 任务编排、ID 选择、平台选择、时间安排 |
| 输入 | 自然语言意图、content_id 列表 |
| 输出 | 任务指令（platform + txt_id + img_id） |
| 特性 | 大模型驱动，无状态，可分身 |

**关键原则**：调度层只发指令，不碰内容，不碰执行。

### 3. 执行层（Worker Layer）

| 项目 | 说明 |
|------|------|
| 职责 | 登录、发帖、返回结果 |
| 输入 | 命令行参数（platform + txt_id + img_id） |
| 输出 | 标准格式（RESULT:success/fail:...） |
| 特性 | 死代码，无智能，只认参数 |

**关键原则**：执行层不接受大模型指令，只认参数。

---

## 二、责任边界

```
内容层负责：生成内容、审核内容、存储内容
调度层负责：任务编排、ID 选择、平台选择
执行层负责：登录、发帖、返回结果
监控层负责：记录状态、存储成功案例、供查询
```

**层与层之间只通过标准化接口通信，不越权。**

---

## 三、数据流向

```
内容层  →  content_id  →  调度层
调度层  →  任务指令    →  中间层（监管层）
中间层  →  调用参数    →  执行层
执行层  →  执行结果    →  中间层
中间层  →  判断调度    →  成功记录 / 失败重试
调度层  ←  最终结果    ←  中间层
```

---

## 四、组合规则

一个小事件 = 调度层 + 监管层 + 执行层

```
事件：发一条抖音帖子
├── 调度层：选择 content_id=5，平台=抖音，时间=现在
├── 监管层：接收指令 → 调用 Worker → 判断结果 → 记录/重试
└── 执行层：python worker.py --platform douyin --txtid 5 --imgid 3 --auto
```

多个小事件 = 大事件

```
大事件：多平台推广
├── 小事件1：抖音发帖
├── 小事件2：Facebook 发帖
└── 小事件3：TikTok 发帖
```

多个大事件 = 更大事件

```
更大事件：全天推广计划
├── 大事件1：早间推广（3平台 × 5条）
├── 大事件2：午间推广（3平台 × 5条）
└── 大事件3：晚间推广（3平台 × 5条）
```

---

## 五、大模型分身调度

同一个大模型 API 可以包装成 N 个无状态调度分身：

| 特性 | 说明 |
|------|------|
| 无状态 | 所有状态由中间层维护，分身不存状态 |
| 无锁 | 分身之间不通信、不共享状态、互不感知 |
| 线性扩展 | 增加分身数量 = 线性增加任务吞吐量 |

---

## 六、监控反馈闭环

监控层记录每次执行的结果，提供查询接口：

```
GET /api/success_cases?limit=10
```

大模型可以查询"历史成功案例"，优化调度策略：

```
调度 → 执行 → 记录 → 反馈 → 调度优化
```

---

## 七、项目文件

| 文件 | 说明 |
|------|------|
| middleware.py | 监管层（中间层） |
| worker_template.py | 执行层（Worker 模板） |
| scheduler_demo.py | 调度层（单实例演示） |
| multi_scheduler_demo.py | 调度层（分身演示） |
| content_layer_demo.py | 内容层（独立演示） |
